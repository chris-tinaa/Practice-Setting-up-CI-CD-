# --- identifiers -------------------------------------------------------
sonar.projectKey=chris-tinaa_Practice-Setting-up-CI-CD-
sonar.organization=chris-tinaa
sonar.projectName=Practice-Setting-up-CI-CD-
sonar.projectVersion=1.0

# --- analysis scope ----------------------------------------------------
# analyse only the actual code (tests remain visible but don’t skew duplication metrics)
sonar.sources=weather-report/src

# exclude just the two flaky specs from *coverage* (they stay in analysis)
sonar.coverage.exclusions=weather-report/src/weatherService.test.ts,\
                          weather-report/src/weatherController.test.ts

# --- coverage reports --------------------------------------------------
# TypeScript / JavaScript
sonar.javascript.lcov.reportPaths=coverage/lcov.info
# JVM (we have none) – silence JaCoCo sensor
sonar.coverage.jacoco.xmlReportPaths=
